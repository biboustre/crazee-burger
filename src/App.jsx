import "./App.css";
import LoginPage from "./components/pages/login/LoginPage";

function App() {
  // state (état, données)
  
  // Comportement (fonctions, effets)

  // affichage (render)
  return (
    <LoginPage />
  );
}

export default App;
